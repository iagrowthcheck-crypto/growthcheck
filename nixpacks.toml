[phases.install] 
cmds = ["pip install -r requirements.txt"] 
 
[phases.build] 
cmds = ["find . -name '*.pyc' -delete", "find . -name '__pycache__' -delete"] 
 
[start] 
cmd = "uvicorn main:app --host 0.0.0.0 --port 8080" 
